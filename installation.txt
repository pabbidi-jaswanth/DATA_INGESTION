Standardized column names (lowercase, snake_case).

Mapped hostel table headers consistently.

Detected currency (INR/USD), occupancy & AC categories.

Converted PG fees (cat1/cat2) into a single common table format.

Removed duplicate rows and checked for missing or wrong values (basic validation).
