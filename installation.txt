Module-1: Data Ingestion → PDF → staging CSVs (pdfplumber, Camelot).

Module-2: Preprocessing → curated processed CSVs (fees, eligibility, documents, hostel).

Module-3: Embeddings & Indexing → heading-aware chunks → FAISS index.

Module-4: Static Retrieval (Basic) → SQL router can fetch basic tables/answers, but exact retrieval formatting & polish still pending.

Outputs:

Data/staging/*.csv (auto-extracted)

Data/processed/** (curated truth)

Data/processed/chunks_clean_final.jsonl (for embeddings)
